import React from "react";

const MadlibsResult = ({ noun1, noun2, adjective, color }) => {
	return `There was a ${color} ${noun1} who loved a ${adjective} ${noun2}`;
};

export default MadlibsResult;
